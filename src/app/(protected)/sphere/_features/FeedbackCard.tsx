import React from 'react';

const FeedbackCard = () => {
  return (
    <div className="h-60 w-full overflow-hidden text-ellipsis rounded-md border bg-background p-6 text-sm text-muted-foreground">
      Every Supabase project comes with a full Postgres database, a free and
      open source database which is considered one of the world&apos;s most
      stable and advanced databases. Features# Table view# Y
    </div>
  );
};

export default FeedbackCard;
